{
  "name": "@social-media/shared-types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for frontend and backend",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
